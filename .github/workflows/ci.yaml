name: Continuous Integration

on: [push]

jobs:
  test:
    name: Run tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout commit
        uses: actions/checkout@v2
      - name: Install python
        uses: actions/setup-python@v2
        with:
          python-version: "3.8.3"
      - name: Cache packages
        uses: actions/cache@v1
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-py-3.8.3-${{ hashFiles('requirements/*.txt') }}-${{ hashFiles('setup.py') }}
      - name: Install dependencies
        run: make setup
      - name: Lint
        run: make lint
      - name: Run unit tests
        run: make test_unit
      - name: Start minikube
        run: |
          curl -LO https://storage.googleapis.com/minikube/releases/latest/minikube-linux-amd64
          sudo install minikube-linux-amd64 /usr/local/bin/minikube
          sudo minikube start --kubernetes-version="1.14.10" --driver="none"
          sudo chown -R $USER $HOME/.kube $HOME/.minikube
      - name: Install helm
        run: |
          sudo apt -y install socat
          curl https://raw.githubusercontent.com/kubernetes/helm/master/scripts/get | bash -s -- -v v2.16.7
          helm init --wait
      - name: Run integration tests
        run: make test_integration
