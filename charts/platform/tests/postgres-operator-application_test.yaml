suite: test postgres operator application
release:
  name: platform
  namespace: platform
templates:
  - postgres-operator-application.yaml
values:
  - common-values.yaml
tests:
  - it: should match snapshot
    asserts:
      - matchSnapshot: {}
