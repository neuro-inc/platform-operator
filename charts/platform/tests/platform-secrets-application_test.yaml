suite: test platform secrets application
release:
  name: platform
  namespace: platform
templates:
  - platform-secrets-application.yaml
values:
  - common-values.yaml
set:
  jobs:
    namespace:
      name: platform-jobs
tests:
  - it: should match snapshot
    asserts:
      - matchSnapshot: {}
