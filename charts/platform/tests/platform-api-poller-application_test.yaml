suite: test platform api poller application
release:
  name: platform
  namespace: platform
templates:
  - argocd-applications/platform-api-poller-application.yaml
values:
  - common-values.yaml
set:
  storages:
    - path: /storage
      size: 10Gi
tests:
  - it: should match snapshot
    asserts:
      - matchSnapshot: {}
