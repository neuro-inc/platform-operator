suite: test platform buckets application
release:
  name: platform
  namespace: platform
templates:
  - platform-buckets-application.yaml
values:
  - common-values.yaml
tests:
  - it: should match snapshot
    asserts:
      - matchSnapshot: {}
