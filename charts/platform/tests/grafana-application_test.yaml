suite: test grafana application
release:
  name: platform
  namespace: platform
templates:
  - grafana-application.yaml
values:
  - common-values.yaml
tests:
  - it: should match snapshot
    asserts:
      - matchSnapshot: {}
