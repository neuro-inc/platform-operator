suite: test platform reports application
release:
  name: platform
  namespace: platform
templates:
  - argocd-applications/platform-reports-application.yaml
values:
  - common-values.yaml
tests:
  - it: should match snapshot
    asserts:
      - matchSnapshot: {}
