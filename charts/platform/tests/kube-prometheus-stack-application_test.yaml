suite: test kube-prometheus-stack application
release:
  name: platform
  namespace: platform
templates:
  - kube-prometheus-stack-application.yaml
values:
  - common-values.yaml
tests:
  - it: should match snapshot
    asserts:
      - matchSnapshot: {}
