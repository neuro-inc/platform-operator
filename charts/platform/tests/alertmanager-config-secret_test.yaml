suite: test alertmanager config secret
release:
  name: platform
  namespace: platform
templates:
  - alertmanager-config-secret.yaml
values:
  - common-values.yaml
tests:
  - it: should match snapshot
    asserts:
      - matchSnapshot: {}
