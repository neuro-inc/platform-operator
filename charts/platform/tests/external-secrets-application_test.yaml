suite: test external secrets application
release:
  name: platform
  namespace: platform
templates:
  - argocd-applications/external-secrets-application.yaml
values:
  - common-values.yaml
tests:
  - it: should match snapshot
    asserts:
      - matchSnapshot: {}
