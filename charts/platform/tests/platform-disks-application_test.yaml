suite: test platform disks application
release:
  name: platform
  namespace: platform
templates:
  - platform-disks-application.yaml
values:
  - common-values.yaml
tests:
  - it: should match snapshot
    asserts:
      - matchSnapshot: {}
