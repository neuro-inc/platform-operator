suite: test loki application
release:
  name: platform
  namespace: platform
templates:
  - argocd-applications/loki-application.yaml
values:
  - common-values.yaml
tests:
  - it: should match snapshot
    asserts:
      - matchSnapshot: {}
