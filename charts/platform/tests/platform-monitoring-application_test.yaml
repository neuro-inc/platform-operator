suite: test platform monitoring application
release:
  name: platform
  namespace: platform
templates:
  - platform-monitoring-application.yaml
values:
  - common-values.yaml
tests:
  - it: should match snapshot
    asserts:
      - matchSnapshot: {}
